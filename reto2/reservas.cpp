/*#include "reservas.h"
#include <iostream>
#include <fstream>
#include <sstream>
#include <cstring>
reservas() {}

reservas(const char* fecha, int dur, const char* codRes, const char* codAloj, const char* docUsu,
         const char* metodo, const char* fechaPay, int montoP, const char* nota) {
    strcpy(fechaEntrada, fecha);
    duracion = dur;
    strcpy(codReserva, codRes);
    strcpy(codAlojamiento, codAloj);
    strcpy(docUsuario, docUsu);
    strcpy(metodoPago, metodo);
    strcpy(fechaPago, fechaPay);
    monto = montoP;
    strcpy(anotaciones, nota);
}
void mostrarComprobante() {
    std::cout << "Reserva: " << codReserva << ", Usuario: " << docUsuario << ", Monto: $" << monto << std::endl;
}

























/*reservas(const char* fecha, int dur, const char* codRes, const char* codAloj, const char* docUsu,
         const char* metodo, const char* fechaPay, int montoP, const char* nota) {
    strcpy(fechaEntrada, fecha);
    duracion = dur;
    strcpy(codReserva, codRes);
    strcpy(codAlojamiento, codAloj);
    strcpy(docUsuario, docUsu);
    strcpy(metodoPago, metodo);
    strcpy(fechaPago, fechaPay);
    monto = montoP;
    strcpy(anotaciones, nota);
}
void mostrarComprobante() {
    std::cout << "Reserva: " << codReserva << ", Usuario: " << docUsuario << ", Monto: $" << monto << std::endl;
}*/
